[package]
name    = "ventura"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "ventura"
path = "src/main.rs"

[profile.dev]
panic = "abort"

[profile.release]
opt-level = "s"    # optimise for size
lto       = true
panic     = "abort"
strip     = "none" # keep symbols for debugging
